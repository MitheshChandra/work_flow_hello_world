name: print-name
on:
        push:
                branches: [ main ]
jobs:
        greet:
                runs-on: ubuntu-latest
                steps:
                        - name: Say Hello
                          run: echo "Hello from print-name workflow!"

